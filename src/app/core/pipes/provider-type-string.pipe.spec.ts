import { ProviderTypeStringPipe } from './provider-type-string.pipe';

describe('ProviderTypeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ProviderTypeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
